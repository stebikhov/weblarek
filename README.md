# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Данные

В ходе анализа проектной работы были выявлены две сущности, которые описывают данные: товар и покупатель. Их можно описать такими интерфейсами:

- товар;
- покупатель.

#### Товар

Интерфейс товара описывает структуру данных товара в каталоге:

```typescript
interface IProduct {
  id: string;
  description: string;
  image: string;
  title: string;
  category: string;
  price: number | null;
}
```

_Поля интерфейса:_

- `id: string` - строковое поле с идентификатором товара.
- `description: string` - строковое поле с описанием товара.
- `image: string` - строковое поле, содержащие url картинки товара.
- `title: string` - строковое поле с наименование товара.
- `category: string` - строковое поле, содержащие категорию товара.
- `price` - числовое поле, содержащее стоимость товара. Может быть null.

#### Покупатель

Интерфейс покупателя описывает данные, необходимые для оформления заказа:

```typescript
interface IBuyer {
  payment: TPayment;  - поле типа `TPayment`, содержит способ оплаты.
  email: string | null; - строковое поле с email клиента.
  phone: string | null; - строковое поле с телефоном клиента.
  address: string | null; - строковое поле с адресом клиента.
}
```

_Поля интерфейса:_

- `payment: TPayment` - поле типа `TPayment`, содержит способ оплаты.
- `email: string` - строковое поле с email клиента.
- `phone: string` - строковое поле с телефоном клиента.
- `address: string` - строковое поле с адресом клиента.

#### Способ оплаты

Тип способа оплаты содержит одно из трех вариантов.

```typescript
type TPayment = "card" | "cash" | null;
```

#### Список товаров

Интерфейс списка товаров, получаемых с сервера:

- `items: IProduct[]` - поле типа массив `IProduct`, содержит список товаров, полученных с сервера.

#### Заказ

Интерфейс заказа, отправляемого на сервер:

```typescript
interface IOrder {
  items: string[]; - поле типа массив строк, содержит список id товаров, оформленных покупателем.
  payment: TPayment; - тип способа оплаты
  address: string;  - строковое поле с адресом клиента.
  email: string;  - строковое поле с email клиента.
  phone: string; - строковое поле с телефоном клиента.
  total: number;  - числовое поле с общей стоимостью корзины.
}
```

#### Статус оформления заказа

Интерфейс ответа сервера по оформленному покупателем заказу:

```typescript
interface IOrderResponse {
  id: string; - строковое поле, содержит идентификатор заказа.
  total: number; - числовое поле, содержит общее кол-во списанных синапсов.
}
```

### Модели данных

#### Класс ProductCatalog

Отвечает за хранение и управление всеми доступными товарами в магазине.

_Поля класса:_

- `products: IProduct[]` — массив товаров.
- `selectedProduct: IProduct | null` — товар, выбранный для детального просмотра.

_Методы:_

- `setProducts(products: IProduct[]): void` — Устанавливает список продуктов
- `getProducts(): IProduct[]` — Возвращает текущий список продуктов
- `getProductById(id: string): IProduct | undefined` — Находит продукт по ID
- `selectProduct(product: IProduct): void` — Устанавливает выбранный продукт
- `getSelectedProduct(): IProduct | null` — Возвращает выбранный продукт
- `clearSelectedProduct(): void` — Сбрасывает выбранный продукт
- `hasSelectedProduct(): boolean` — Проверяет, выбран ли какой‑либо продукт

#### Класс ShoppingCart

Корзина с товарами, которые пользователь выбрал для покупки.

_Поля:_

- `items: IProduct[]` — массив товаров в корзине.

_Методы:_

- `addItem(item: IProduct): void` — Добавляет товар в корзину
- `removeItemById(id: string): boolean ` — Удаляет товар из корзины по ID
- `hasItem(id: string): boolean` — Проверяет наличие товара в корзине
- `getItems(): IProduct[]` — Возвращает список товаров в корзине
- `getItemCount(): number` — Возвращает количество товаров в корзине
- `getTotalPrice(): number ` — Рассчитывает общую стоимость товаров в корзине
- `clear(): void ` — Очищает корзину полностью

#### Класс Buyer

Класс для хранения и валидации данных покупателя.

_Поля:_

- `data: IBuyer` — данные покупателя

_Методы:_

- `update(data: Partial<IBuyer>): void ` — сохраняет данные покупателя.
- `getData(): IBuyer ` — возвращает данные покупателя.
- `clear(): void ` — очищает данные покупателя.
- `validate(): Record<keyof IBuyer, string> ` — выполняет валидацию данных, возвращает результат проверки.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.


# Слой представления (View)
Для корректного отображения данных сайта необходимо реализовать следующие классы: Header, Gallery, Modal, Basket, Card, CardCatalog, CardPreview, CardBasket, Form, OrderForm, ContactsForm, Ordered.




## Header

Компонент шапки сайта, отвечающий за отображение кнопки корзины и счётчика товаров в ней.

#### Назначение

Класс `Header` управляет элементами шапки сайта: кнопкой корзины и счётчиком количества добавленных товаров. При клике на кнопку корзины генерирует событие `basket:open` через шину событий.

#### Наследование

Класс расширяет базовый компонент `Component<IHeader>`.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`

Создаёт экземпляр компонента Header.

- `eventsIEvents`Шина событий для межкомпонентного взаимодействия

- `container` HTMLElementКорневой DOM-элемент шапки

#### Публичные методы и свойства
- `set counter(value: number)` - Устанавливает значение счётчика товаров в корзине.

#### Генерируемые события
- `basket:open` - Генерируется при клике на кнопку корзины


## ContactsForm

Класс представляет форму для ввода контактных данных (email и телефона пользователя). 

#### Назначение

Инициализирует форму, находит DOM‑элементы полей ввода (email и телефон), настраивает обработчики событий и подписывается на события валидации.


#### Наследование

Класс расширяет базовый компонент `Form<TContactsForm>`.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`

Создаёт экземпляр компонента ContactsForm.

- `eventsIEvents`Шина событий для межкомпонентного взаимодействия

- `container` HTMLElementКорневой DOM-элемент шапки

#### Публичные методы и свойства
- `set emailValue(value: string | null)` - Устанавливает значение в поле ввода email.

- `set phoneValue(value: string | null))` - Устанавливает значение в поле ввода телефона.

- `validateForm(errors: IErrors)` - Выполняет валидацию формы и обновляет её состояние.


#### Генерируемые и обрабатываемые события
- `contacts:submit` - при клике на кнопку отправки, если кнопка не заблокирована.

- `form:errors` - передаёт ошибки валидации в форму для обработки.

- `order:change` - отправляется при каждом изменении поля ввода.

## OrderForm

Класс формы оформления заказа, отвечающий за выбор способа оплаты и ввод адреса доставки. Наследуется от базового класса `Form`.


#### Наследование

Класс расширяет базовый компонент `Form<TOrderForm>`.

#### Конструктор
`constructor(events: IEvents, container: HTMLElement)`

Создаёт экземпляр компонента OrderForm.

- `eventsIEvents`Шина событий для межкомпонентного взаимодействия

- `container` HTMLElementКорневой DOM-элемент шапки

#### Публичные методы и свойства
- `set payment(value: TPayment)` - Устанавливает значение в поле способа оплаты.

- `set addressValue(value: string | null)` - Устанавливает значение в поле адреса.

- `validateForm(errors: IErrors)` - Выполняет валидацию формы и обновляет её состояние.


#### Генерируемые и обрабатываемые события
- `order:next` - при клике на кнопку отправки, если кнопка не заблокирована.

- `form:errors` - передаёт ошибки валидации в форму для обработки.

- `order:change` - отправляется при каждом изменении поля ввода.













## Card

Базовый компонент карточки товара. Отображает основную информацию о товаре: название и цену. Служит родительским классом для более специализированных карточек.



#### Конструктор
`constructor(container: HTMLElement)`

Создаёт экземпляр компонента Card.

- `container` HTMLElementКорневой DOM-элемент шапки

#### Публичные методы и свойства
- `get id(): string` - Возвращает идентификатор карточки.

- `set id(value: string)` - Устанавливает идентификатор карточки. Синхронизирует значение с атрибутом id контейнера для возможности поиска элемента в DOM.

- `set title(value: string)` - Устанавливает название товара.

- `set price(value: number | null)` - Устанавливает цену товара. Если цена равна null, отображается текст «Бесценно». Иначе — числовое значение с единицей измерения («синапсов»).





#### Генерируемые и обрабатываемые события
В данном компоненте явная работа с событиями (отправка или обработка) не реализована.



## Компоненты карточек товара

#### Общая информация

Три класса карточек, наследующих базовую логику от `Card`:
- `BasketCard` — карточка в корзине (номер + кнопка удаления).
- `CatalogCard` — карточка в каталоге (категория + изображение).
- `PreviewCard` — превью товара (описание + кнопка действия).

#### Конструктор (общий для всех)

```typescript
constructor(events: IEvents, container: HTMLElement)
```
Параметры:

- `events` - брокер событий (IEvents).

- `container` - корневой элемент карточки (HTMLElement).

#### Методы и события
***


#### CardBasket
Методы:

- `set index(value: number)` — устанавливает порядковый номер.

События:

- `card:delete` — при удалении (данные: { card: string }).
***


#### CardCatalog
Методы:

- `set category(value: string)` — устанавливает категорию.

- `set image(value: string)` — устанавливает изображение (SVG → PNG + CDN).

События:

- `card:open` — при клике на карточку (данные: { card: string }).
***


#### CardPreview
Методы:

- `set category(value: string)` — устанавливает категорию.

- `set image(value: string)` — устанавливает изображение.

- `set description(value: string)` — устанавливает описание.

- `set inCart(value: boolean)` — обновляет состояние кнопки («Купить»/«Удалить»).

- `disableButton()` — деактивирует кнопку (если товар недоступен).

События:

- `card:delete` — при нажатии «Удалить из корзины» (данные: { card: string }).

- `card:add` — при нажатии «Купить» (данные: { card: string }).

Примечания
Обрабатываемые события: отсутствуют.

Наследование: все классы расширяют Card (базовые поля: title, price).

---
## Компоненты интерфейса

#### Basket (Корзина)

**Назначение:** управление отображением товаров в корзине, общей стоимости и кнопки оформления заказа.

**Конструктор:**
```typescript
constructor(events: IEvents, container: HTMLElement)
```
- `events` — брокер событий.

- `container` — корневой элемент корзины.

Публичные методы:

- `set items(nodes: HTMLElement[])` — обновляет список товаров, управляет CSS‑классами и состоянием кнопки.

- `set total(value: number)` — устанавливает общую стоимость (в «синапсах»).

События:

Отправляемые: `basket:ready` — при клике на кнопку оформления заказа.

---
#### Gallery (Галерея)
Назначение: рендеринг списка карточек товаров в каталоге.

Конструктор:

```typescript
constructor(container?: HTMLElement)
```
- `container` — контейнер галереи (если не передан, ищет .gallery).

Публичные методы:

- `set catalog(items: HTMLElement[])` — заменяет содержимое галереи на переданный массив элементов.

---
#### Header (Шапка)
Назначение: отображение кнопки корзины и счётчика товаров.

Конструктор:

```typescript
constructor(events: IEvents, container: HTMLElement)
```
- `events` — шина событий.

- `container` — корневой элемент шапки.

Публичные методы:

- `set counter(value: number)` — обновляет значение счётчика товаров.

События:

Отправляемые: `basket:open` — при клике на кнопку корзины.

---
#### Modal (Модальное окно)
Назначение: управление открытием, закрытием и содержимым модального окна.

Конструктор:

```typescript
constructor(events: IEvents, container: HTMLElement)
```

- `events` — система событий.

- `container` — корневой элемент модального окна.

Публичные методы:

- `open()` — открывает окно (добавляет класс modal_active).

- `close()` — закрывает окно и очищает содержимое.

- `set content(element: HTMLElement)` — устанавливает новое содержимое.

---
#### Ordered (Экран подтверждения заказа)

Назначение: отображение результата успешного оформления заказа.

##### Конструктор:

```typescript
constructor(events: IEvents, container: HTMLElement)
```

- `events` — система событий.

- `container` — корневой элемент компонента.

##### Публичные методы:

- `set total(value: number)` — устанавливает сумму списанных синапсов в описании.

События:

Отправляемые: `success:closed` — при клике на кнопку закрытия.

##### Общие замечания: все классы расширяют Component, который обеспечивает базовую работу с DOM и состоянием.













































***


# События в приложении

Система событий построена на принципах реактивного программирования и управляет полным жизненным циклом процесса покупки — от просмотра каталога до завершения транзакции. Все события генерируются автоматически, обеспечивая синхронизацию между слоями компонентов без прямых командных обращений.

### 🎯 1. Навигация и выбор товаров в каталоге

**`catalog:changed`**  
Каталог товаров обновлен или фильтры изменены. Событие триггерит перерисовку списка карточек товаров с учетом новых параметров поиска, сортировки или пагинации.

**`card:open`** → **`catalog:changed`**  
Клик по карточке товара. Открывает модальное окно с детальной информацией о товаре и его текущей ценой. Последнее событие запускает пересчет стоимости при изменении характеристик (цвет, размер).

**`card:add`** → **`basket:changed`**  
Товар успешно добавлен в корзину. Обновляет счетчик товаров в иконке корзины и инициирует пересчет общей стоимости.

**`card:delete`** → **`basket:changed`**  
Удаление конкретного товара из корзины. Немедленно отражается в интерфейсе общего количества и стоимости.

### 🛒 2. Работа с корзиной покупок

**`basket:open`**  
Открытие панели корзины для просмотра содержимого. Переводит хедер в режим "оформление заказа".

**`basket:changed`**  
Общее событие изменения состава корзины. Запускается после `card:add`, `card:delete` или ручного изменения количества товаров. Синхронизирует состояние во всех компонентах.

**`basket:ready`** → **`order:next`**  
Корзина полностью готова к оформлению — все товары выбраны, количество определено, общая стоимость актуальна. Разблокирует кнопку "Оформить заказ" и активирует первый шаг процесса.

### 📝 3. Оформление заказа и валидация

**`order:change`** → **`form:errors`**  
Любое изменение в полях формы заказа (адрес доставки, ФИО, телефон, email). Событие передает актуальные значения в модель данных и мгновенно запускает валидацию.

**`form:errors`**  
Возврат результатов валидации. Если данные некорректны, событие передает структурированный список ошибок для подсветки полей и отображения подсказок пользователю. Если все поля валидны — форма готова к отправке.

**`order:next`**  
Пошаговая навигация по wizard'у оформления заказа:  
1. Метод оплаты → 2. Контактные данные → 3. Подтверждение.  
Переходируется только при успешной валидации текущего шага.

**`contacts:submit`** → **`success:closed`**  
Финальная отправка заказа на сервер. Передает полную модель данных (товары, контакты, платежные данные). Блокирует интерфейс до ответа сервера.

### ✅ 4. Завершение транзакции

**`success:closed`**  
Окно успешного заказа закрыто пользователем. Финализирует весь цикл:  
- Очищает корзину полностью  
- Возвращает пользователя в каталог товаров  
- Сбрасывает все состояния формы  
- Разблокирует возможность новых покупок

## 🔄 Поток выполнения (упрощенный)

```mermaid
catalog:changed → card:open → card:add → basket:changed
         ↓                                       
basket:open → basket:ready → order:next → contacts:submit
         ↓                           
order:change → form:errors → success:closed
```




https://github.com/stebikhov/weblarek
